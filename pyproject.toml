[project]
name = "animate-x"
version = "0.1.0"
description = "Local-first daily batch controller for Wan Animate style video jobs."
requires-python = ">=3.11"
dependencies = [
    "boto3>=1.34.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
